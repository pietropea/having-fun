# UI of the Having Fun tool

## Local development

From the `./ui` folder, install the project dependencies with one of the following command:

```
$ npm install

// or

$ yarn
```

Now run start the local development server using:

```
$ npm run dev
```

